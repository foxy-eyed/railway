<h1><%= t('.title') %></h1>

<table class="table table-striped">
  <thead>
  <tr>
    <th><%= t('admin.tickets.attributes.sn') %></th>
    <th><%= t('admin.tickets.attributes.train') %></th>
    <th><%= t('admin.tickets.attributes.route') %></th>
    <th><%= t('admin.tickets.attributes.start_station') %></th>
    <th><%= t('admin.tickets.attributes.end_station') %></th>
    <th><%= t('admin.tickets.attributes.buyer') %></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <%= render @tickets %>
  </tbody>
</table>
