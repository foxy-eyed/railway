<h1><%= t('.title') %></h1>
<div class="row">
  <div class="col-sm-12">
    <%= link_to t('common.add'), new_admin_train_path,
                class: 'btn btn-primary btn-lg pull-right', role: 'button' %>
  </div>
</div>
<table class="table table-striped">
  <thead>
  <tr>
    <th><%= t('admin.trains.attributes.number') %></th>
    <th><%= t('admin.trains.attributes.route') %></th>
    <th><%= t('admin.trains.attributes.current_station') %></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <%= render @trains %>
  </tbody>
</table>
