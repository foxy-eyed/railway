<h1><%= t('.title') %></h1>

<%= render 'form', wagon: @wagon %>

<p class="nav-inner">
  <%= link_to t('common.show'), admin_wagon_path(@wagon) %>
  <span> | </span>
  <%= link_to t('common.back'), admin_train_wagons_path(@wagon.train) %>
</p>